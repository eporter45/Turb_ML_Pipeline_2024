from setuptools import setup, find_packages

setup(
    name="spring_research",   # any name you like
    version="0.0.1",
    packages=find_packages(),     # auto‑discovers Preprocessing/, Models/, etc.
)
